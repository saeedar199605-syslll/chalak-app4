# گزارش اصلاح ذخیره‌سازی ابری و باگ‌گیری — نسخه ۲.۱

تاریخ بررسی: ۱۴۰۵/۰۶/۲۴ (۲۰۲۶-۰۹-۱۵)

## نتیجه

نسخه ۲.۱ برای اجرای Cloudflare Pages آماده شده و مشکل اصلی همگام‌سازی میان مرورگرها رفع شده است. ذخیره محلی دیگر به‌عنوان ذخیره موفق ابری نمایش داده نمی‌شود و دادهٔ قدیمی یک مرورگر نمی‌تواند کل داده‌های جدید مرورگر دیگر را بازنویسی کند.

## اصلاحات اصلی

- فعال‌شدن همگام‌سازی فقط پس از احراز هویت؛ جلوگیری از ارسال دادهٔ نمونه یا محلی پیش از ورود.
- ارسال تغییرهای کلیدی به‌جای بازنویسی کامل state و نگهداری revision روی سرور.
- ادغام امن تغییرهای دو مرورگر و دریافت دوره‌ای دادهٔ تازه از Cloudflare KV.
- حفظ تغییرهای ارسال‌نشده هنگام قطع شبکه و تلاش مجدد خودکار.
- نمایش وضعیت واقعی ذخیره ابری، در حال همگام‌سازی و خطای اتصال در رابط.
- واکنش رابط فعال به داده‌ای که از مرورگر دیگر رسیده است.
- اضافه‌شدن endpoint سلامت برای کنترل وجود و عملکرد binding با نام `CHALAK_DB`.
- ذخیره ابری برای تنظیمات، مجوزها، قفل کاربران، رویدادها، تیکت‌ها، پاداش، OKR، جلسات، تمجید و داده‌های بهره‌وری.

## رمز و نشست

- رفع خطای JavaScript در پاسخ‌های خالی، HTML یا نامعتبر API رمز.
- هش PBKDF2/SHA-256 برای رمزهای اختصاصی؛ رمز خام در state ذخیره نمی‌شود.
- تنظیم، تولید، بازنشانی و مشاهده وضعیت رمز اختصاصی هر کاربر.
- انتقال اعتبارنامه هنگام تغییر نام کاربری.
- بی‌اعتبارشدن نشست‌های قبلی حساب پس از تغییر یا بازنشانی رمز.
- تغییر امن رمز مدیر و الزام ورود مجدد همه نشست‌های قبلی مدیر.
- پاک‌سازی اعتبارنامه و ارزیابی‌های وابسته هنگام حذف گروهی کاربر.

## پوشش مدیریت مدیر

کنترل ویرایش/حذف برای کارکنان، شاخص‌ها، پروفایل‌ها، ارزیابی‌ها، گردش کار، آرشیو، پاداش، OKR و نتایج کلیدی، جلسات و اقدامات ۱به۱، تمجیدها، داده‌های بهره‌وری، اهداف کارگاهی، رویدادهای تقویم، تیکت‌ها و پاسخ‌های تیکت تکمیل یا بازبینی شد. حذف مدیر اصلی عمداً مسدود است.

## آزمون‌ها

- TypeScript type-check: موفق
- تست خودکار: ۱۷ تست در ۵ فایل، همگی موفق
- ساخت production با Vite: موفق
- کامپایل Cloudflare Pages Functions: موفق
- بررسی امنیت وابستگی‌ها: صفر آسیب‌پذیری شناخته‌شده
- بررسی رابط: دکمه‌های ویرایش/حذف جدید حاضر و خطای کنسول JavaScript ثبت نشد
- تست عملی دو نشست مستقل Cloudflare KV: موفق

سناریوی تست عملی شامل ذخیره در مرورگر A، دریافت در مرورگر B، تغییر یک بخش در B، تغییر بخش دیگری با نسخه قدیمی A بدون حذف داده B، ورود با رمز اختصاصی کاربر، بازنشانی به کد پرسنلی، تغییر رمز مدیر و ردشدن نشست‌های قدیمی بود.

## استقرار ضروری

1. در Cloudflare Pages یک KV namespace به binding دقیق `CHALAK_DB` متصل شود.
2. secretهای `ADMIN_PASSWORD` و در صورت استفاده `GEMINI_API_KEY` تنظیم شوند.
3. پس از هر تغییر binding یا secret یک deployment تازه انجام شود.
4. بعد از انتشار، `/api/health` باید `storage: workers-kv` و `configured: true` برگرداند.

جزئیات کامل اجرا و استقرار در `README.md` داخل بسته قرار دارد.
